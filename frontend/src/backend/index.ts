import { mockBackend } from "../mocks/backend"
import type { FileManagerBackend } from "./contracts"

// as i didnt start the backend yet, i will use the mock for now
export const backend: FileManagerBackend = mockBackend
